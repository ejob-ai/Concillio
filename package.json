{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "build:css": "postcss src/styles/tailwind.css -o public/static/tailwind.css --env production",
    "dev": "npm run build:css && wrangler pages dev dist --d1=concillio-production --local --ip 0.0.0.0 --port 3000",
    "build": "npm run build:css && vite build",
    "preview": "npm run build:css && wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run build && wrangler pages deploy dist --project-name concillio",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name concillio",
    "deploy:pages": "wrangler pages deploy dist --project-name concillio --commit-dirty=true",
    "test": "npm run build:css && npm run build && vitest run",
    "test:policy": "vitest run --reporter=dot -t 'Policy: no Tailwind CDN'",
    "legacy:status": "node -e \"(function(){try{const cutoff=new Date('2025-10-02');const now=new Date();const after=now>=cutoff;console.log('[legacy]', 'cutoff:', cutoff.toISOString(), 'now:', now.toISOString(), 'after_cutoff:', after); console.log('[legacy]', 'Reminder: check __legacyMenuTriggered__ in a real browser DevTools>Application>Local Storage after this date. If 0 -> remove legacy menu code & CSS.');}catch(e){console.error(e)}})()\""
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "hono": "^4.9.5",
    "scrypt-js": "^3.0.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@types/node": "^24.3.1",
    "autoprefixer": "^10.4.21",
    "json5": "^2.2.3",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.17",
    "vite": "^6.3.5",
    "vitest": "^2.1.9",
    "wrangler": "^4.37.1"
  }
}
