name: Update STATUS timestamp

permissions:
  contents: write
  actions: read
  pull-requests: read

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["E2E (preview)", "E2E Smoke on main"]
    types: [completed]

jobs:
  update:
    if: github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          persist-credentials: true

      - name: Update STATUS.md timestamp + write status.json
        run: |
          set -euo pipefail
          TS="$(date -u +'%Y-%m-%d %H:%M UTC')"
          sed -i "s|<!--STATUS_TS-->.*<!--/STATUS_TS-->|<!--STATUS_TS-->${TS}<!--/STATUS_TS-->|" STATUS.md
          echo "{\"updated\":\"${TS}\"}" > status.json

      - name: Commit + push [skip ci]
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add STATUS.md status.json
          git commit -m "docs: update STATUS timestamp + json [skip ci]" || echo "nothing to commit"
          git push origin HEAD:main
